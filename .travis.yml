language: cpp

compiler: gcc

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq build-essential cmake

install: ./scripts/get-linux-deps.sh

script: mkdir -p build && cd build && cmake -DBUILD_TEST=true .. && make

test: build/TestIniFileCpp
